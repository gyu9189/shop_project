package com.dangjang.repository;

import com.dangjang.domain.EatenTrash;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EatenTrashRepository extends JpaRepository<EatenTrash, Long> {

}
